Excel Formulas

1. For creating the Group Key:

for output
=TRIM(CLEAN(TEXT(A2, "yyyy-mm-dd ") & TEXT(A2, "hh:mm") & "_" & P2 & "_" & I2))
=IF(COUNTIF(,R2)>0,1,0)

for gp
=TRIM(CLEAN(TEXT(L2, "yyyy-mm-dd ") & TEXT(M2, "hh:mm") & "_" & K2 & "_" & H2))
=IF(COUNTIF(,P2)>0,1,0)

yyyy-mm-dd HH:MM:SS
=TEXT(L2, "yyyy-mm-dd")
=VALUE(P2)

column sequence for creating GP_key should be like this: 
for GP - yyyy-mm-dd + hh:mm + approval code + Terminal number
for accounting raw/ output - yyyy-mm-dd hh:mm + approval code + Terminal number

column headers for upload to gp merchant spreadsheet
Merchant Number	Merchant Name	Original Transaction Amount	Original Transaction Amount Currency	Transaction Type	Payment Method	Card Number	Terminal Number	Settlement Amount	Settlement Amount Currency	Authorization Code	Transaction Date	Transaction Time	File Date	File Time
