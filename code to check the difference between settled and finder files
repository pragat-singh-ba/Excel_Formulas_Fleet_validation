import pandas as pd

# Define the directory where the files are located
directory_path = r'C:\Users\

# Initialize a variable to store the total sum of 'Settled Amount'
total_settled_amount = 0

# Iterate through all files in the directory
for file_name in os.listdir(directory_path):
    if file_name.endswith('.xlsx'):  # Check if the file is an Excel file
        file_path = os.path.join(directory_path, file_name)
        df = pd.read_excel(file_path)
        if 'Settled Amount' in df.columns:  # Check if the column exists
            total_settled_amount += df['Settled Amount'].sum()

# Calculate the difference between the total settled amount and sum_file1
difference = sum_file1 - total_settled_amount

# Round the difference to 2 decimal places
difference_rounded = round(difference, 2)

# Print the results
print(f"Total Settled Amount in all files: {total_settled_amount}")
print(f"Difference: {difference}")
print(f"Difference (rounded): {difference_rounded}")
